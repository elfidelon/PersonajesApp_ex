<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base de la Activity. Jetpack Compose dibuja toda la UI,
         este estilo solo controla la ventana (status bar, fondo previo al
         primer frame de Compose, etc.) -->
    <style name="Theme.PersonajesApp" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowFullscreen" tools:targetApi="29">false</item>
    </style>
</resources>
